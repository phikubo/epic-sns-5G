Aquí se colocan los scripts donde se desarrolla por primera vez. 
Luego pasan a la fase de pruebas y experimentación.

La justificación de esta estructura se encuentra en el reporte #20. La carpeta ventana
hace referencia a interfaz en el anterior prototipo.

En la carpeta raiz se encuentran tres modulos:
1. gui
2. pruebas
3. sistema

Consideraciones:
a. gui implementa las funciones de sistema como se ha hecho hasta
    el momento.
b. sistema es una copia de pruebas. Pruebas es la primera fase de desarrollo
    luego de experiementar se pasa la funcion en cuestion a sistema.
c. en este prototipo se omite la palabra prot correspondiente a prototipo.
d. las carpetas si deben distinguirse cuando sean paquetes o no, con la
    palabra: pk_<nombre_paquete> en la carpeta.